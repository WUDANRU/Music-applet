/* @import "iconfont.wxss"; */   /* 全局样式在组件内用不了，需要吧它拷贝到组件内(search下的iconfont.wxss)，并且import引入 */ 
/* 组件是components的文件，比如search组件   全局样式(miniprogram下的iconfont.wxss)就是在组件内用不了，在pages下的文件都能用 */  

.container {
  display: flex;
  align-items: center;
  margin-left: 20rpx;
  position: relative;
}

.bar {
  flex: 1;
  height: 68rpx;
  font-size: 28rpx;
  border-radius: 50px;
  background-color: #f5f5f5;
  padding-left: 60rpx;
}

button.search {
  background: #d43c33;
  color: #fff;
  margin-left: 20rpx;
  font-size: 24rpx;
  width:160rpx;
  height:26px;
}

.in-bar {
  color: #999;
}

 .find { /* .icon-sousuo这个全局的类在pages下的可以用，在components组件下用不了 重新在组件内定义了新的类名.find */
  position: absolute; /* 如果用的是这个@import "iconfont.wxss";可以用.icon-sousuo这个类名 */
  top: 50%;
  transform: translate(16rpx, -50%);
  color: #9f9f9f;
}
